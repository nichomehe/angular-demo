import { DoubleNumPipe } from './double-num.pipe';

describe('DoubleNumPipe', () => {
  it('create an instance', () => {
    const pipe = new DoubleNumPipe();
    expect(pipe).toBeTruthy();
  });
});
